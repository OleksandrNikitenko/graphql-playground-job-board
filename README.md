# GraphQL Job Board
This project uses Apollo Server with Express, and Apollo Client as GraphQL clients. 
The application is used to practice queries, mutations, custom object types, authentication, etc.